# 나중계획 (2026-08-01 보류 — 항목별로 실행 완료 시 해당 섹션 삭제, 전부 끝나면 파일 삭제)

# [1] 장비 교체 후 COM 맵핑 복구

> **호출 방법**: 장비 전원+USB 연결 후 Claude에게 "나중계획 실행" 이라고 하면
> 아래를 한 번에 수행한다. **전부 반영 완료 후 이 파일은 삭제한다.**

## 배경
- 분취기(홀더/장비) 교체 후 "맵핑이 풀리고 에러" 발생 보고 (2026-08-01, 에러 원문 미확보)
- 진단 시점 이 PC의 COM 포트 0개 → 실측 불가로 보류
- 구조 분석 완료: 진단 도구 `diagnose_mapping.py` 작성됨 (앱과 동일 매칭 로직 사용)

## 확인된 취약점 (config 구조 분석)
1. **96well 분취기 `dev_plate96_1` (COM15)** — VID/PID 미등록 = 하드코딩 전용.
   장비 교체·재연결로 COM 번호 바뀌면 즉사. ★1순위 용의자.
   보드 = Arduino Mega + FTDI FT232R (예상 VID:PID `0403:6001`, 실측으로 확정할 것)
2. **ESP32 밸브 5ch `dev_esp32v_1~5` (COM7 공유)** — VID/PID 미등록 (STATIC)
3. CH340 `1A86:7523` 8대 공유 (Chemyx 4 + Runze 4, SN 없음) — probe 의존
4. 히터 SN `AWATB147612` 고정 — 어댑터 교체 시 폴백 강등

## 실행 절차 (호출 시 순서대로)
1. `py -3.14 diagnose_mapping.py` (프로브 포함) — MISS 장치·미청구 라이브 포트 실측
2. `hardware_config.json` 백업 (`.bak_YYYYMMDD_HHMMSS`)
3. MISS 장치의 port 를 새 COM 번호로 수정
4. 분취기: 실측 VID/PID(/SN) 를 inventory 에 등록 → 자동매칭으로 전환
   (ESP32 밸브도 실측 VID/PID 나오면 같이 등록 권장)
5. `diagnose_mapping.py` 재실행 → 역할사용 장치 전부 AUTO/정상 확인
6. 앱 부팅 → 연결 로그에서 실연결 확인 (특히 `[Collector] Connected`)
7. **이 파일(나중계획.md) 삭제**

## 참고
- 사용자가 봤던 에러 메시지 원문을 받으면 1번 전에 대조할 것
- 분취기 신규 좌표는 이미 반영됨(공칭 계획값) — 티칭(`calibrate_deck_v13.py`)은 별도 건
- Manual 탭 위상센서 CAL 버튼 추가됨 — OPB 센서 연결 시 config 에 phase 역할 등록 필요 (별도 건)

---

# [2] 레벨센서 raw vs median 비교 캘리브레이션

> **호출**: "레벨센서 비교 캘리 실행". [1] 맵핑 복구 후에 할 것 (Chemyx 포트 필요).

## 목적
"median 없이 그냥 측정" vs "현재 구현(호스트 median-of-N)" 을 동일 조건에서 비교, curve 포함.

## 핵심 설계 (확정)
- median 은 펌웨어가 아니라 **호스트 계산** (`ultrasonic_level._read_raw`) → 원시 스트림
  1회 기록 후 두 추정자를 **같은 에코 데이터**에서 도출 = 현재 구현과 수학적으로 동일하면서
  런 간 교란(온도/메니스커스/마운트) 제거된 진짜 동일 조건.
- 덤으로 median-of-5(고속 모드)/10(구 캘리)/20(현재 기본) 동시 비교.

## 도구 (작성·합성데이터 검증 완료)
`calibrate_level_median_compare.py`
- 실측: `py -3.14 calibrate_level_median_compare.py --pump COMx` (센서 COM3, CH0)
  - 주사기 수동 0 정렬 → [0→5→0]×1, 0.3mL 스텝, 스텝당 원시 40표본 기록
- 재분석만: `--replay level_cmp_raw.csv` / 파이프라인 데모: `--sim`
- 출력: `level_cmp_raw_<타임스탬프>.csv`(원시 전체, 실측마다 별도 보존) +
  `level_cmp_report_*.png`(4패널: 커브/오차/추정자 박스/왕복차) +
  `level_cmp_summary_*.txt`(추정자별 평균|err|·SD·P95·최대 µL, 100µL 게이트 판정, fit 계수)
  ※ 무타임스탬프 3파일(level_cmp_raw.csv 등)은 --sim 데모 산출물
- ⚠ `--diameter` 실물 각인 확인 후 지정 (백로그 3-A: config 12.45 vs 실물 5mL 주사기 미확정)
- 유효표본 < 10 스텝은 자동 제외+경고 (드라이버 품질게이트와 동일 취지)

## 실행 절차 (호출 시)
1. [1] 완료 확인 (Chemyx/센서 포트 확정)
2. 주사기 0 정렬 안내 → 스크립트 실측 실행 (약 35스텝, 이동+측정 ~30분)
3. 리포트 판독: raw 1샷 P95 vs median-of-20 P95, 100µL 게이트, 히스테리시스
4. fit 계수가 현재 config(slope 1.06776/intercept -9.2382)와 유의미하게 다르면
   hardware_config.json 캘리 값 갱신 제안 (백업 후)
5. **이 섹션 삭제**

---

# [3] deck_v13 실측 티칭 (분취기 좌표 확정)

> **호출**: "데크 티칭 실행". 분취기 COM 확정([1]) + 데크 물리 장착 후.
> ⚠ 현재 well_coordinates.json 은 **공칭 계획값** — 티칭 전 분주 실행 금지(검증 이동만).

1. 데크 정렬·장착: 니들 센터크로스 G1 X100 Y93.94 / 회전 y=0선 G1 X40↔X160 Y93.94
   → M3 4점 마킹 → 탭/T너트 → 체결 (볼트중심 허용창 X 82.5~117.5 / Y 70.1~117.8)
2. `py -3.14 calibrate_deck_v13.py` — 5점 티칭(L-A1/L-H12/R-A1/R-H12/RES_CENTER)+z_travel
   (자동 백업, 잔차>0.3mm·회전>0.5°·포락선 초과 시 경고)
3. `--verify` 검증 이동 (대표 웰 6 + RES 2, 육안 확인)
4. Manual 탭 A12→B1 행전환 소요시간 로그 → `min_tube_sec`/`collector_move_lead_sec` 상향
   (백로그 3-B — 플레이트 간격 91.98mm 로 변경돼 재실측 필수)
5. 플레이트 실물 확정: 기존 2mL 딥웰 유지면 그대로 / 얕은 플레이트(웰깊이 10.9mm)면
   collector_plate96.max_volume_per_well_ml 1.5 → 실측 용량으로 인하 (안 하면 넘침)
6. **이 섹션 삭제**

---

# [4] 위상센서(OPB/OCB350) 실기 셋업

> **호출**: "위상센서 셋업 실행". 센서 아두이노 USB 연결 후.

1. 포트 확인 → hardware_config.json 등록:
   inventory: `{"id":"dev_phase_1","name":"위상센서","driver":"위상센서 어레이 (OCB350)",
   "port":"COMxx","settings":{"sensors":{"collect":0}}}` + roles: `"phase":{"driver_id":"dev_phase_1"}`
2. 앱 부팅 → Manual 탭 SLUG PHASE 행 + **CAL 버튼** 노출 확인 (2026-08-01 추가됨)
3. 빈 튜브(N2 퍼지) 상태에서 CAL 실행 → 직후 GAS 검증 통과 확인 (버튼에 내장)
4. 액체 주입해 배지 액체/기체 전환 + 아날로그값 확인
5. (별도 승인 후 구현) 엔진 D 가드: 시퀀스 세척 N2 퍼지 중 read_phase()==GAS 검증,
   실패 시 경고+그 시점(기체 보장) 자동 재캘리 — strict_engine 변경이라 계획 제시→승인 필요
6. **이 섹션 삭제**
