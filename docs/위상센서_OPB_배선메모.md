# 위상센서 (OPB 포토인터럽트) 배선·전환 메모 — 2026-08-05

> **🔴 2026-08-17 실기 확정 (이 블록이 아래 예시보다 우선)**
>
> | 이름(사용자 명명) | 위치 | 물리 채널 | threshold | 확정 근거 |
> |---|---|---|---|---|
> | **센서1** | INLET — 가스T→반응기 입구 앞 (`reactor_in`) | **A0 (ch0)** | 440 | 액체 찬 INLET 튜브 안착 시 ch0=855~878(물800) 실측 |
> | **센서2** | OUTLET — 반응기 출구→아웃렛 직전 (`collect`) | **A1 (ch1)** | 717 | 동시 ch1=495~499(공기457) |
>
> - 포트 **COM19** (CH340, 시리얼번호 없음 — 재열거 시 포트 갱신 필요. 실제로 08-18 재배선 중 COM18→COM19 재열거됨), config `dev_opb_1`
>
> **🔧 2026-08-18 전체 배선 완료 + 확장 펌웨어 업로드 (실배선 확정 핀맵)**
>
> | 선 색 | 기능 | 센서1 (INLET) | 센서2 (OUTLET) |
> |---|---|---|---|
> | 흰색 | Analog | **A0** | **A1** |
> | 주황 | OUT A | D2 | D4 |
> | 파랑 | OUT B | D3 | D5 |
> | 초록 | **Calibrate** | **D6** | **D7** |
> | 빨강/검정 | 5V/GND | 공통 | 공통 |
>
> - ⚠ **RoboChem 표준 핀맵과 다름** (표준: 유닛0=Cal D2/B D3/A D4, 유닛1=Cal D5/B D6/A D7).
>   모드 B(Phase_Sensor_Array.ino) 전환 시 그 펌웨어의 핀 define 을 이 표로 수정 필수.
> - **현행 펌웨어 = `hardware/arduino/opb_phase_cal/opb_phase_cal.ino`** (08-18 업로드, arduino-cli).
>   스트림 포맷 동일(`S1:adc,p | S2:adc,p`) — 단 p = OCB350 디지털 3상 판정(0에러/1투명/2불투명/3기체).
>   `CAL`/`CAL1`/`CAL2` 명령 = Cal 핀 100ms 펄스 (응답 `CAL:OK,x`). 드라이버 `calibrate()`가 전송.
>   ~~N2Precal 이 배기 완료 시점에 자동 호출~~ → **🔴2026-08-18 정책 반전: 자동 CAL 기본 off**
>   (`n2_precal_auto_cal: false`). RoboChem 원본 재확인 결과 매런 자동 CAL 은 우리 확장이었음 —
>   OmniPlatypus 에서 위상센서 calibrate 는 수동 대화형 스크립트(platform_calibration.py,
>   사람이 빈 라인 육안 확인 후 실행) 1곳뿐, 실험 시퀀스 0건. 자동판은 '비었음'을 캘리브 대상
>   센서 자신의 GAS 판정으로 보증하는 순환 논리 → 유색액(임계 아래=GAS 오판) 위에서 CAL
>   오발사 → 보드 기준 오염(센서2 상시-0 사고, 트레이스의 원점 195±171 증거) + 매런 재생산.
>   **수동 절차 = `tools/opb_manual_cal.py`** (앱 종료 후 루트에서 실행: 육안 확인 → CAL 전후
>   ADC 표집·비교). CAL 후에는 레벨맵 이동 가능 — 공기/물/유색 반응액 재실측 후 임계 갱신 필수.
>
> **🟢 2026-08-18 저녁 수동 CAL 복구 실행 결과 (빈 관 육안 확인 후)**
>
> | 채널 | CAL 전 공기 | CAL 후 공기(안정) | 판독 |
> |---|---:|---:|---|
> | ch0 센서1 | 520.8 ±1.2 (정상이었음) | **521 ±1.3**, p=3(기체)✓ | 오염 없었음 — 액체CAL 피해는 센서2만 |
> | ch1 센서2 | **229.0 ±1.2 (오염 확정 증거)** | **511 ±1.0** (구 기준 519 근접 복구) | CAL 2회(2발째가 501→511 미세 보정) |
>
> - 액체 위 CAL 오염 가설 **실측 확정**: 복구 전 센서2 공기=229 (球 기준 519와 290 차이,
>   σ1.2로 '안정적으로 틀린' 상태 = 기준점 자체가 액체에 맞춰져 있었음).
> - CAL 직후 ~수 초는 재영점 스윕으로 ADC 요동(0 포함) — 표집은 3s+ 안정화 후에.
> - ⚠ **센서2 펌웨어 판정 p=1(투명액체) 고착** — 공기인데 OUT B(파랑, D5)가 계속 LOW.
>   아날로그·Cal 핀은 정상 동작하므로 파랑선 D5 접촉/보드 B출력 점검 대상.
>   **모드 A(PC 임계)는 p를 안 쓰므로 현행 운용 무영향** — p(3상) 진실원 전환 전에만 수리 필요.
> - **물 실측 확정(같은 날 저녁)**: 센서2 액체 **983.2 ±0.5** (앱 종료 후 정밀 표집 n=121)
>   / 센서1 액체 990 (UI 로그). 같은 표집에서 센서1은 521(공기)·p=3 — 펌프 정지 시점에
>   INLET 센서 지점에 기포가 앉아 있던 것(직전 에지 1→0 ADC 683과 정합), 고장 아님.
>   → 복구 후 현행 진실: **ch0 공기 521 / 물 990, thr 690** (마진 169/301) ·
>   **ch1 공기 511 / 물 984, thr 750** (마진 239/234 — 중간값 747.5 에 사실상 정확).
>   **임계 690/750 유지 확정.** 위 15:17 재캘리브 표는 이 표로 대체.
> - 센서1이 펌프 기동/정지 때 0↔1 진동하며 중간 ADC(617~900)로 에지 찍는 것은
>   기포 슬러그 경계 통과 중 디바운스 커밋 시점의 중간값 — 정상 동작.
> - 남은 실측: **유색 반응액** — 실런에서 센서2 값 관찰, 750 아래로 깎이면
>   p(3상) 전환 + 센서2 파랑선(D5) 수리 논의.
> - ⚠ 하드웨어 캘리브는 LED 구동을 바꿔 **ADC 레벨을 이동**시킴 → 캘리브 후 공기/물 기준이
>   달라질 수 있음. N2Precal 원점 재표집이 자동으로 뒤따르며, wet 실측 후 임계 재조정은
>   하드웨어 다이얼로그 위치분담 UI에서.
> - ⚠ 아래 §모드A 의 settings **예시**({collect:0, reactor_in:1})는 구 가정 — 실배선은 **반대**.
>   현행 config = `{"reactor_in": 0, "collect": 1}` / thresholds `{"reactor_in": 440, "collect": 717}`
>   (임계값은 '채널'의 물성이므로 채널을 따라감)
> - **실기 펌웨어 출력은 CSV 가 아니라 라벨 포맷** `S1:adc,판정 | S2:adc,판정` —
>   드라이버 파서가 두 포맷 모두 수용하도록 확장됨(2026-08-17, phase_sensor_opb.py).
>   펌웨어 자체판정 숫자는 무시, 진실원 = PC측 threshold (모드 A 설계 불변).
> - ⚠ **fail-unsafe**: 두 채널 다 '튜브 빠짐'이 액체로 읽힘(ch0 없음574>440, ch1 없음960>717).
>   단독 신뢰 금지 — 장착 상태 wet/dry 재캘리브 권장. 채널↔위치 재배정은 하드웨어
>   다이얼로그 **Phase Sensor 페이지의 위치 분담 UI**(2026-08-17 신설)에서.
>
> **🔴 2026-08-18 재배선 후 실측 재캘리브 — 아래 표가 현행 진실 (구 벤더표 폐기)**
>
> | 채널 | 공기(실측) | 액체(실측) | 없음 | **threshold (중간값)** |
> |---|---:|---:|---:|---:|
> | ch0 (센서1 INLET) | **522** | 855~878 | 553 | **690** |
> | ch1 (센서2 OUTLET) | **519** | 977~982 | 960 | **750** |
>
> - 발단: 재배선 후 공기가 522 로 읽히는데 구 임계 440 이 **공기보다 아래** →
>   센서1이 공기를 영구 LIQUID 오판 (2026-08-18 실기 확인, config 갱신 완료)
> - **CAL 펄스는 '디지털 판정'만 재영점 — 아날로그 레벨은 불변** (실측: CAL 전후
>   공기 522→522). 즉 임계 재산정은 CAL 로 대체 불가, 이 표처럼 실측으로만.
> - ⚠ ch0 은 '없음(553)'과 공기(522)가 Δ31 — **미장착 검출 불가** (N2Precal 도
>   ch0 미장착 검사 스킵). ch1(Δ441)은 검출 가능.
> - 유색 시약 주의는 여전: 액체 신호가 임계(690/750) 아래로 깎이면 GAS 오판 —
>   최암색 반응액 실측 후 임계 하향.
>
> **🟢 2026-08-18 시약 선단 N2 마커 (InjMarker) — 구현·배선 완료 (모의 검증, 실기 대기)**
>
> - 원리: OPB 는 용매/시약을 구별 못 하므로(둘 다 맑은 액체) **도징 시작과 동시에**
>   가스T 로 N2 펄스 1개를 주입 — 마커는 가스T 에서 즉시 출발, 시약 선단은 주입경로
>   통과(pre_sec) 후 가스T 도착 → **센서2의 [마커 꼬리 0→1 시각] + pre_sec = 시약 선단
>   실측** (결정론, 모델 추정 아님). 기록·검증 전용, 제어 무개입 (5단계 비전의 3번 A단계).
> - 발사 지점: `S{n}-Injection` 도징의 on_pumps_started 콜백(타이머 resume 과 동일
>   시점)에서 데몬 스레드 — `strict_engine._inject_front_marker` 참조. hte_mode 는
>   자체 스페이서 체계라 제외(분기 앞에서 자동 배제).
> - 설정(system_params): `inj_marker_enabled`(기본 **false**) / `inj_marker_sec`(1.0)
>   / `inj_marker_sccm`(0 = n2_precal_sccm 폴백). 10 sccm×1s ≈ 170 µL(표준환산).
> - 마커 통과 시점 Outlet=WASTE (COLLECT 는 t_head 이후) → 수집물 무영향.
>   실패는 경고·스킵, MFC OFF 는 finally 페일세이프.
> - 모의 검증(2026-08-18): mock MFC 로 balanced 시나리오 — 펌프 스타트와 갭 0.00s,
>   펄스 폭 1.01s, 종료 후 0 sccm, 기존 회귀(liquid_front·collect_mapping·deadvol) 전 통과.
> - 실기 절차: `inj_marker_enabled: true` → 런 후 로그의 `[PhaseEdge] 센서2` 1→0(마커
>   머리)·0→1(마커 꼬리) 시각 확인 → 꼬리+pre_sec 를 `[Timer] HEAD` 와 대조.
>
> **🟢 2026-08-18 밤 — 브래킷 마커 + MarkerCollectGate (센서 트리거 분취) 구현**
>
> - **사용자 확정 로직(RoboChem)**: `[용매][N2전단마커][화합물][N2후단마커][push용매]` —
>   센서2가 ①전단마커 꼬리(G→L=화합물 선두)→수집 시작 ②후단마커 머리(L→G=꼬리 통과)
>   →WASTE 절단 + ①~② 간격=슬러그 통과시간 실측.
> - **`inj_marker_mode`**: `off` / `t0`(구 방식, 도징 개시 발사·기록 전용, 갭=pre_sec 판독,
>   `inj_marker_enabled:true` 하위호환) / **`bracket`**(전단=펌핑경과 pre_sec, 후단=도징종료
>   +pre_sec — 슬러그 양끝 밀착, 타이머 lane "marker" 이벤트로 pause 안전).
> - **`marker_gate_mode`**: `off` / `observe`(검출·기록만) / **`gate`**(제어): ①검출 시
>   `CollectionTimer.shift()` 재앵커 — 밸브 직접 조작 없이 기존 집행 기계(HEAD·preflush·
>   웰 이동·waste_guard) 재사용. 조기 도달=HEAD 즉시 발화(완전 센서 트리거), 지연 도달=
>   HEAD 는 이미 열림(선용매 희석, Δ 재현 시 t_head 모델 재보정) + 잔여 이벤트 지연 보정.
>   ②검출 시 valve_lag 후 WASTE 직접 절단 — terminal WASTE 는 시간제 폴백으로 유지.
> - 안전 계약: 검출 실패=전부 시간제 폴백 / 조기 재앵커 `marker_gate_max_early_sec`(10s)
>   클램프(분취기 이동 보호) / 후단 절단은 front+0.5×슬러그 이전 무장 금지(반토막 방지) /
>   게이트 무장 시 HeadProbe 자동 강등(read_event 소비형 큐 소유권 단일화) / push 창은
>   게이트 shift 도 연장 반영.
> - ⚠ shift 클록 결합: 후단 앵커는 `front_el − applied_shift`(신클록 환산) — 모의 E2E 에서
>   놓친 꼬리로 발견·수정. `tests/test_marker_bracket_gate.py` 18항목 ALL PASS
>   (fallback/late+6/early−11 클램프/t0 호환) + 회귀 5종 통과.
> - **도입 순서(실기)**: `inj_marker_mode:"bracket"`+`marker_gate_mode:"observe"` 수 런
>   → Δ 재현 확인 → `"gate"`. 유색 크루드가 임계(750) 아래로 읽히면 G→L 에지가 안 보임
>   — 그 경우 p(3상) 전환+D5 수리 선행 (전단마커 '진입만 감지' 로그가 그 신호).
>
> **🟢 2026-08-19 — "parked"(정지선 발사) 모드 추가·기본 채택 (사용자 확정)**
>
> - 동기: 브래킷은 흐르는 액체에 가스를 끼워 넣는 공류 주입이라 세그멘터 효과로
>   마커가 쪼개질 수 있음(특히 후단 = Reaxus 맥동 캐리어) — 사용자 지적.
> - **원리**: 마커를 '흐름이 없는 순간'에 발사해 가스T 에 주차 — **전단 = 시약 장전
>   완료 직후(전 펌프 정지)**, **후단 = 주입종료~push 무유량 갭**. 흐름 재개 시 마커가
>   T+0 에 출발, 화합물 경계는 주입경로 pre_sec 뒤에 따라옴 →
>   **판독 = 마커 꼬리 에지(G→L) + pre_sec** (결정론적 오프셋, 주입경로는 실측 원장).
> - 장점: ①단일 플러그 보장(쪼개짐 원천 차단) ②전단마커 앞뒤가 무색 용매 스페이서라
>   **유색 크루드에도 서명 선명** ③잡기포 구분 = **기체 지속 ≥ marker_gate_min_gas_sec**
>   (2s, 마커 2.5s 발사 전제 — 2026-08-19 사용자 지시 "여유있게 2~3초").
> - 주의: ①후단 절단은 반드시 꼬리에지+pre 뒤 — 주입경로 잔류 화합물(pre 분량) 유실
>   방지(테스트로 고정) ②후단 마커는 수집 경계 안쪽에 들어가 웰에 기포 1개 포함(무해)
>   ③주차 주입이 본류를 마커 부피만큼 밀어 t_head 소폭 조기화 — gate 재앵커가 흡수,
>   observe 에선 Δ<0 로 관측됨.
> - **후단 1차 센서 = 센서1 (2026-08-19 사용자 확정)**: 후단 마커는 push(Reaxus 맥동)
>   캐리어로 반응기를 5분간 지나며 쪼개질 수 있으므로, **반응기 진입 전 센서1에서
>   갓 형성된 단일 플러그를 실측**하고 절단 시각을 계산한다:
>   `절단 = 센서1 마커꼬리 + pre_sec(원장) + 가스T→센서2 수송(전단마커 실측) + 밸브지연`
>   — 반응기 모델 불사용. 센서2는 **크로스체크**(Δ 로그, 재현 시 재보정 신호),
>   센서1 실패 시 센서2 승격, 최후엔 terminal WASTE 시간제 폴백. 절단이 terminal 보다
>   늦어지는 지오메트리(수집 여유 < 절단 바이어스)는 폴백이 수행하고 실측만 기록.
>   절단 바이어스 = 마커 통과폭+가스T→센서1 소구간 (늦은 쪽 = push 용매 소량 포함,
>   유실 없음 — 크로스체크 Δ로 정량).
> - 설정: `inj_marker_mode:"parked"`(현행) / `inj_marker_sec: 2.5` /
>   **`inj_marker_sccm: 3.0`**(10은 과속 — 사용자, 3sccm×2.5s ≈ 표준 125µL) /
>   `marker_gate_mode:"observe"`(현행, gate 승격 대기) / `marker_gate_min_gas_sec: 2.0`
>   / `marker_gate_rear_sensor_key:"reactor_in"`.
>   test_marker_bracket_gate **30항목 ALL PASS** (bracket 회귀 + parked 잡기포 기각·
>   센서1 실측·크로스체크·terminal 선행·폴백).

같은 센서 하드웨어(OPB 포토인터럽트 + TT 캘리브레이션 보드)로 **두 가지 운용 모드**를
오갈 수 있다. 앱 계약(read_phase/monitor/wait_edge)이 동일해서 하드웨어 다이얼로그의
**드라이버 라벨만 바꾸면 전환**된다. 코드 수정 불필요.

---

## 모드 A — ADC 스트림 (현행, 동료 리그 = Photo_Interrupt.zip)

- **드라이버 라벨**: `위상센서 (OPB ADC 2ch)` → `hardware/sensors/phase_sensor_opb.py`
- **펌웨어**: `Photo_Interrupt/Arduino/PhotoSensor.ino` (UNO, 115200bps)
- **배선**: 센서1 아날로그 출력→A0, 센서2→A1, 캘리브 보드 VCC→5V, GND→GND
  (⚠ 아날로그 출력 와이어는 색으로 추정하지 말고 라벨 확인 — 벤더 README 경고)
- **프로토콜**: 50ms마다 `adc1,adc2\r\n` CSV 상시 스트림 (요청-응답 아님)
- **판정**: PC측 임계값. ADC > threshold = 액체(1) / 이하 = 기체(0). 2상만.
- **캘리브레이션**: 절차 없음 — 고정 임계값. 장치 settings 예:
  ```json
  "settings": {"sensors": {"collect": 0, "reactor_in": 1},
               "thresholds": {"collect": 440, "reactor_in": 717}}
  ```
- **벤더 실측표 (2026-08-05 초기값 — 튜브/정렬 변경 시 재측정)**:

  | 채널 | 튜브없음 | 공기 | 물 | threshold | 액체신호 감쇄 허용 |
  |---|---:|---:|---:|---:|---|
  | S1(A0) | 574 | 80 | 800 | 440 | 물 대비 45%까지 |
  | S2(A1) | 960 | 457 | 977 | 717 | 물 대비 27%만 — **취약** |

- **유색/진한 시약 주의**: 액체 신호가 흡수로 깎여 임계값 아래로 내려오면 기체 오판.
  → 가장 진한 반응액으로 ADC 실측 후, 부족하면 threshold 를 공기값과 그 액체값의
  중간으로 하향. 그래도 안 되면 모드 B 전환.

## 모드 B — RoboChem 3상 스택 (색/불투명 대응 내장 — 예비)

- **드라이버 라벨**: `위상센서 어레이 (OCB350)` → `hardware/sensors/phase_sensor_array.py`
- **펌웨어**: `robochem_devices/_reference/firmware/Phase_Sensor_Array/` (UNO, 9600bps)
  ※ 플래시 시 앱 종료(포트 점유). UNO 1대 = 최대 4센서.
- **배선(센서당)**: 캘리브 보드의 **디지털 A/B 출력**→UNO 디지털 핀, Cal 핀→UNO 출력
  핀, 아날로그 출력→아날로그 핀(진단용). 핀 배정은 펌웨어 Phase_Sensor_Array.ino 참조.
- **판정**: **캘리브 보드 하드웨어**가 3상 판정 — 0=ERROR/1=투명액체/2=불투명·유색
  액체/3=기체 를 A+2B 2비트로 출력 (OCB350.cpp read()). 진한 시약 = OPAQUE_LIQUID 로
  잡히므로 색 변화에 구조적으로 강건. 엔진 is_liquid 는 GAS 아니면 전부 액체.
- **캘리브레이션**: 캠페인 시작 시 **빈 튜브(기체) 상태에서 1회** — 드라이버
  `calibrate()` = 보드 Cal 핀 100ms 펄스(보드 내부 기준 재설정). 액체 상태에서
  실행 금지(판정 뒤집힘).
- settings 의 `thresholds` 키는 이 드라이버에서 무시됨(무해) — 전환 시 지울 필요 없음.

## 전환 절차 (A→B)

1. 앱 종료 → RoboChem 펌웨어 플래시 (9600bps)
2. 캘리브 보드 디지털 A/B/Cal 핀을 UNO에 배선 추가
3. 하드웨어 다이얼로그: 해당 장치 드라이버를 `위상센서 어레이 (OCB350)` 로 변경
   (settings.sensors 는 그대로 유지됨)
4. 앱 시작 → 빈 튜브 상태에서 calibrate 1회

역방향(B→A)은 PhotoSensor.ino 재플래시 + 라벨 변경만.
